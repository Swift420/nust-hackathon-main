@page
@model schoolAdmin.Pages.Loads.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Workloads[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Workloads) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.WorkloadID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.WorkloadID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.WorkloadID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
